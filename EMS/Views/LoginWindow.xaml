<Window x:Class="EMS.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EMS.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="250" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtUsername" Grid.Row="0" Margin="100,0,0,0" Width="200"/>
        
        <TextBlock Text="Password" Grid.Row="1" VerticalAlignment="Center"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Margin="100,0,0,0" Width="200"/>

        <Button x:Name="btnLogin" Content="Log In" Grid.Row="2" Margin="0,20,0,0" Width="100" HorizontalAlignment="Center" Click="btnLogin_Click"/>
    </Grid>
</Window>
