<Window x:Class="EMS.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EMS.Views"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="400" MinHeight="250" MinWidth="300"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Border Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Welcome to Environment Management System" Grid.Row="0" Grid.ColumnSpan="2" FontSize="16" FontWeight="Bold" 
                           TextAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="200" Margin="0,0,0,20"/>

                <TextBlock Text="Username:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtUsername" Grid.Row="1" Grid.Column="1" MinWidth="150"/>

                <TextBlock Text="Password:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,10,0"/>
                <PasswordBox x:Name="txtPassword" Grid.Row="2" Grid.Column="1" MinWidth="150" Margin="0,10,0,0" KeyDown="txtPassword_KeyDown"/>

                <Button x:Name="btnLogin" Content="Log In" Grid.Row="3" Grid.ColumnSpan="2" 
                        HorizontalAlignment="Center" Margin="0,20,0,0" Width="100"
                        Click="btnLogin_Click"/>

                <TextBlock x:Name="txtErrorMessage" Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Center" Foreground="Red" Text=""/>
            </Grid>
        </Border>
    </Grid>
</Window>
