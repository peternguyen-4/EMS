<Window x:Class="EMS.Views.ExportDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Data" Height="200" Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Dataset Selection -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Select Data Type:" VerticalAlignment="Center" Width="140"/>
            <ComboBox x:Name="DataTypeDropdown" Width="200"/>
        </StackPanel>

        <!-- Export Button -->
        <Button x:Name="ExportButton" Grid.Row="1" Content="Export" Width="150" HorizontalAlignment="Center" Click="ExportButton_Click" Margin="0,0,0,10"/>

        <Button x:Name="CancelButton" Grid.Row="2" Content="Cancel" Width="150" HorizontalAlignment="Center" Click="CancelButton_Click"/>

    </Grid>
</Window>
